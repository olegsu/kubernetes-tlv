version: '1.0'
mode: parallel
steps:
  step1
    image: alpine:3.8
    commands:
    - cf_export TEST_1=1
    - env

  step2:
    image: alpine:3.8
    commands:
    - cf_export TEST_2=2
    - env
    when:
      steps:
      - name: step1
    
    
  step3:
    image: alpine:3.8
    commands:
    - cf_export TEST_1=2
    - env
    when:
      steps:
      - name: step1    

  step4:
    image: alpine:3.8
    commands:
    - env
    when:
      steps:
      - name: step3    
