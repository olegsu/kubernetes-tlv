apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-test-{{ now | quote }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-test
    image: {{ .Values.testImage }}
    command: ["sh", "-c", "echo", "Ruuning release test"]
  restartPolicy: Never